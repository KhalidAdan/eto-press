import { defineConfig } from "blume";

export default defineConfig({
  content: {
    root: "content"
  },
  description:
    "A local-first printing press for a personal newspaper: each story is one event told through outlets that disagree, every source named, and the brief ends.",
  github: {
    owner: "KhalidAdan",
    repo: "eto-press"
  }
});
